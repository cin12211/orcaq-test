// Full shape defined in core/persist/normalize.ts (normalizeAppConfigState)
// This type is a reference alias — normalize.ts is the single source of truth
export type { AppConfigPersistedState } from '~/core/persist/normalize';
