appId: com.cinny.orcaq
productName: orcaq
copyright: Copyright © 2025–2026 Cinny. All rights reserved.

directories:
  output: out/packages
  buildResources: build

# Optimize bundle size
compression: maximum

# Files to bundle into the app
files:
  - out/electron/**/*
  - '!out/electron/**/*.d.ts'
  - '!out/electron/**/*.map'

# Bundle the Nitro runtime binary as extra resource
extraResources:
  - from: out/runtime/
    to: runtime/
    filter:
      - '**'

# Auto-update: publish to GitHub Releases
publish:
  provider: github
  owner: cin12211
  repo: orca-q
  releaseType: release

# ─── macOS ───────────────────────────────────────────────────────────────────
mac:
  category: public.app-category.developer-tools
  icon: assets/logos/icon.icns
  target:
    - target: dmg
      arch:
        - arm64
        - x64
  # Uncomment when Apple Developer certs are configured:
  # hardenedRuntime: true
  # entitlements: build/entitlements.mac.plist
  # entitlementsInherit: build/entitlements.mac.plist
  # gatekeeperAssess: false
  # identity: null  # Remove when using real cert

dmg:
  contents:
    - x: 130
      y: 220
    - x: 410
      y: 220
      type: link
      path: /Applications

# ─── Windows ────────────────────────────────────────────────────────────────
win:
  icon: assets/logos/icon.ico
  target:
    - target: nsis
      arch:
        - x64
  # Uncomment when Windows EV cert is configured:
  # certificateSubjectName: "Your Company Name"
  # signingHashAlgorithms:
  #   - sha256

nsis:
  oneClick: true
  perMachine: false
  allowToChangeInstallationDirectory: false
  deleteAppDataOnUninstall: false

# ─── Linux ───────────────────────────────────────────────────────────────────
linux:
  icon: assets/logos/
  category: Development
  target:
    - target: AppImage
      arch:
        - x64
    - target: deb
      arch:
        - x64
